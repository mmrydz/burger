<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devour}}">
		{{#if devour}}ORDER{{else}}RESTOCK{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE FROM MENU</button>
</li>